{{- if .Values.checker.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "openstatus.fullname" . }}-checker
  labels:
    {{- include "openstatus.componentLabels" (dict "context" . "component" "checker") | nindent 4 }}
spec:
  type: {{ .Values.checker.service.type }}
  ports:
    - name: http
      port: {{ .Values.checker.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "openstatus.selectorLabels" (dict "context" . "component" "checker") | nindent 4 }}
{{- end }}
