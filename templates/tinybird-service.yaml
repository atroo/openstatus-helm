{{- if .Values.tinybird.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "openstatus.fullname" . }}-tinybird
  labels:
    {{- include "openstatus.componentLabels" (dict "context" . "component" "tinybird") | nindent 4 }}
spec:
  type: {{ .Values.tinybird.service.type }}
  ports:
    - name: http
      port: {{ .Values.tinybird.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "openstatus.selectorLabels" (dict "context" . "component" "tinybird") | nindent 4 }}
{{- end }}
